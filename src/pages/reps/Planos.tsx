/** Dashboard de Representante — Planos e Módulos.
 *  Clonado da aba Planos e Módulos do SDR (mesmo conteúdo, componente compartilhado). */
import { PlaybookPlanos } from '@/components/playbook/PlaybookPlanos';

export default function RepsPlanos() {
  return (
    <div className="p-8">
      <PlaybookPlanos />
    </div>
  );
}

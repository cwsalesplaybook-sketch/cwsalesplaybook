/** Planos e Módulos — desmembrado da aba Sales Enablement pra virar item próprio do menu. */
import { PlaybookPlanos } from '@/components/playbook/PlaybookPlanos';

export default function Planos() {
  return (
    <div className="p-8">
      <PlaybookPlanos />
    </div>
  );
}
